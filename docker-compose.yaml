services:
  laf-api:
    image: ghcr.io/fabriciojm/laf-api:test
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      INFERENCE_HOST: "http://guama:8000"
      INFERENCE_ENDPOINT: "/generate"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
