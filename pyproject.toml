[project]
name = "laf-api"
version = "0.1.0"
description = "An API for laf slow inference"
requires-python = ">=3.12"
dependencies = ["fastapi", "uvicorn[standard]", "pydantic"]

[dependency-groups]
dev = ["pytest", "httpx", "ruff"]
